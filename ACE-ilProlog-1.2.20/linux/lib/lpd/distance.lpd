
name('Support for extra distances').

authors(['Jan Ramon']).

contact(['janr@cs.kuleuven.ac.be']).

load_package(gapic).

load_package('gapic/c_struct').

load_message('Loading distance support').

use_module('distance/distance').

end_of_header.

command(distance_test,distance_test,[],'distance_test','does a short test of the distance functionality',distance_test,distance).
