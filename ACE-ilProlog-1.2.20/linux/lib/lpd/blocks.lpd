
name('Support for the blocks world application').

authors(['Jan Ramon']).

contact(['janr@cs.kuleuven.ac.be']).

load_package(gapic).

load_package(oo_dist).

load_message('Loading blocks world support').

use_module('appl/blocks/blocks').

end_of_header.
