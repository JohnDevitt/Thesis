
name('Support for the multi-agent grid world environment').

authors(['Tom Croonenborghs']).

contact(['tomc@cs.kuleuven.be']).

load_package(rrl3).

load_message('Loading multi-agent rrl grid-world environment').

use_module('appl/multi_agent_gridworld/multi_agent_gridworld').

classdef_module(multi_agent_gridworld).

end_of_header.

setting(multi_agent_gridworld_options,[number_of_states(4),number_of_actions(100),optimal_actions([1,2,3,4])],I,(I = [_|_]),'must be a list').
