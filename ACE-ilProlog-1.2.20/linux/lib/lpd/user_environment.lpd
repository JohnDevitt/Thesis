
name('Support for an environment definition in background').

authors(['Tom Croonenborghs']).

contact(['tomc@cs.kuleuven.ac.be']).

load_message('Loading support for the background environement').

use_module('appl/user_environment/user_environment').

classdef_module(user_environment).

end_of_header.
